---
- hosts: centos
  tasks:

  - name: show hostname
    command: 'id'
    register: result
  - debug: msg="My ID is {{ result.stdout }}" 
  - name: install apache2
    apt: name=apache2 update_cache=yes state=latest


  
