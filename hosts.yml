---
- hosts: centos
  tasks:

  - name: show id
    command: 'id'
    register: result
  - debug: msg="My Id is {{ result.stdout }}" 
  - name: show hostname
    shell: 'df -h'
    register: result
  - debug: msg="My hostname is {{ result.stdout }}" 

    
